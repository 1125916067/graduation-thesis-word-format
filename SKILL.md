---
name: academic-word-format
description: 中文学术格式 Word 文档生成技能。当用户需要创建课程作业、学术论文、读书报告、实验报告、结课论文等中文 Word 文档，并要求规范学术排版（A4、黑体标题、宋体五号正文、1.5 倍行距、首行缩进 2 字符、三线表、页脚页码、参考文献 [n] 自动编号）时使用。提供可直接执行的生成脚本 scripts/build_academic_docx.py，供 AI Agent 或其他工具以 JSON spec 调用，产出统一排版成品。
---

# 中文学术格式 Word 文档

将中文学术文档（课程作业 / 论文 / 报告）排版为统一规范格式。格式规则源于《学术论文编写规则》GB/T 7713.2—2022 的常用中文方案，并经过实际作业验证（题名黑体小二、作者楷体小四、摘要仿宋小五、一级标题黑体小四、二级标题黑体五号、正文宋体五号）。

## 快速开始

依赖：`python-docx`。

```bash
python scripts/build_academic_docx.py --spec spec.json --out 输出.docx
```

`spec.json` 定义题目、署名、摘要、章节、表格与参考文献（完整结构见下文"spec 结构"）。脚本内置全部排版规则，无需再手动设置字体、边距、表格边框或页码。

## spec 结构

```json
{
  "title": "论文题目",
  "author": "李佳润　202622011504　第二次作业",
  "abstract": "摘要文本（可选）",
  "keywords": "关键词1；关键词2（可选）",
  "sections": [
    {"level": 1, "text": "1　引言"},
    {"level": 0, "text": "正文段落……"},
    {"table": 0},
    {"level": 2, "text": "2.1　小节"}
  ],
  "tables": [
    {
      "caption": "表1　标题",
      "headers": ["列1", "列2", "列3"],
      "rows": [["a", "b", "c"]],
      "align": ["center", "left", "left"],
      "widths_cm": [2.8, 6.6, 6.6]
    }
  ],
  "references": ["文献1……", "文献2……"]
}
```

- `sections` 为有序内容块：`level` 0=正文段、1=一级标题、2=二级标题；`{"table": n}` 在当前位置插入 `tables[n]`。
- `tables` 使用三线表（顶底线 1pt、表头下线 0.5pt、无竖线），表头行自动加粗居中、跨页重复、行不跨页。
- `references` 自动生成 Word 原生 `[1] [2] …` 编号列表（悬挂缩进 2 字符、小五号），无需手工编号。
- 省略的字段留空即可；表格列数由 `headers` 决定，`align`/`widths_cm` 不填时自动处理。

## 排版规则摘要

| 角色 | 字体 | 字号 |
|---|---|---|
| 题名 | 黑体（西文 Times New Roman） | 小二 18pt，加粗居中 |
| 作者行 | 楷体 | 小四 12pt，居中 |
| 摘要/关键词引题 | 黑体 | 小五 9pt |
| 摘要/关键词内容 | 仿宋 | 小五 9pt |
| 一级标题 | 黑体 | 小四 12pt，加粗黑色 |
| 二级标题 | 黑体 | 五号 10.5pt，加粗黑色 |
| 正文 | 宋体（西文 Times New Roman） | 五号 10.5pt |
| 表题/表内文字 | 黑体/宋体 | 小五 9pt |
| 参考文献 | 宋体 | 小五 9pt |

页面：A4，四边 2.5cm；正文两端对齐、1.5 倍行距、首行缩进 2 字符、段前后 0；页脚居中阿拉伯页码；标题黑色、无下划线、无主题蓝。完整规则与实现细节见 [format-spec.md](references/format-spec.md)。

## 使用要点

- 标题文本用全角空格分隔编号与标题（如 `1　引言`）；正文用中文引号，禁止英文双引号。
- 需要正文内 `[n]` 上标引用并与参考文献联动跳转（REF 域）时，生成后用 word 技能（若可用）的 citations 工作流处理；本脚本负责格式骨架与参考文献列表。
- 需要编辑已生成的文档时，遵循 word 技能（若可用）的编辑流程，保持本格式的样式继承。
- 生成后用 LibreOffice 渲染 PDF 或打开检查版面；表格行数、列数与 spec 逐项核对。
